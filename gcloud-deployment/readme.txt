To run a full deployment of the transit clock using clusters:
- first run cluster-full-deploy.sh <agency-id>
- wait a few minutes, then run clustered-server-deploy.sh <agency-id>

For all kinds of less structured information, see notes.txt

